// control flow
// for loop
// for (let i = 0; i < 5; i++) {
//     console.log(`in loop: ${i}`);
// } // don't add semicolon after for loop curly braces
// console.log('loop finished');

const names = ['mark', 'cark', 'clark', 'dark'];

for (let i = 0; i < names.length; i++) {
    console.log(`<span>${names[i]}</span>`);
}